name: Manter Site Vivo
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Acordar PythonAnywhere
        run: |
          curl -I https://kevinbrain.pythonanywhere.com/webhook
